// routes/reminder_emailRoutes.js
const express = require('express');
const { processRemindersRoute } = require('../controllers/reminder_emailController');

const router = express.Router();

//router.get('/reminders/trigger', processRemindersRoute);

module.exports = router;

